#-------------------------------------------------------
#   Assignment: Assignment 2
#         Name: Million Eyassu
#-------------------------------------------------------


# Inputs: radius as inches, 
# Output: area
import math 


dblRadius = float(input('Enter the radius for the circle: '))       # gets input from the user and converts it to a float 
dblArea = float((math.pi * (dblRadius * dblRadius)))                # calculates the  with the user input (radius) and converts it into a float 
print("The area of the circle is: %.2f" %dblArea)                   # displays the results




  
